# Profit + transaction + drawdown + volatility + overtrading
reward:
  components:
    profit:
      enabled: true
    holding:
      enabled: false
    volatility:
      enabled: true
    drawdown:
      enabled: true
    transaction:
      enabled: true
    overtrading:
      enabled: true
    scaling:
      enabled: false
    margin:
      enabled: false
    liquidation:
      enabled: false
    constraint_violation:
      enabled: false
